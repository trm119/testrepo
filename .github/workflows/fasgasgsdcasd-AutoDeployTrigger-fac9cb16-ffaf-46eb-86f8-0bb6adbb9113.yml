name: Trigger auto deployment for fasgasgsdcasd

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/fasgasgsdcasd-AutoDeployTrigger-fac9cb16-ffaf-46eb-86f8-0bb6adbb9113.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.FASGASGSDCASD_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.FASGASGSDCASD_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.FASGASGSDCASD_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: 
          registryUsername: ${{ secrets.FASGASGSDCASD_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.FASGASGSDCASD_REGISTRY_PASSWORD }}
          containerAppName: fasgasgsdcasd
          resourceGroup: tmauldin-test
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          


