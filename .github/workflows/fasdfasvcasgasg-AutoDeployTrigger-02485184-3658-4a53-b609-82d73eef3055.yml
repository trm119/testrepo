name: Trigger auto deployment for fasdfasvcasgasg

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/fasdfasvcasgasg-AutoDeployTrigger-02485184-3658-4a53-b609-82d73eef3055.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.FASDFASVCASGASG_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.FASDFASVCASGASG_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.FASDFASVCASGASG_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: 
          registryUsername: ${{ secrets.FASDFASVCASGASG_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.FASDFASVCASGASG_REGISTRY_PASSWORD }}
          containerAppName: fasdfasvcasgasg
          resourceGroup: tmauldin-test
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          


