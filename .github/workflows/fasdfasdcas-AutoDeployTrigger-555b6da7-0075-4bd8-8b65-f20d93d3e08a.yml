name: Trigger auto deployment for fasdfasdcas

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/fasdfasdcas-AutoDeployTrigger-555b6da7-0075-4bd8-8b65-f20d93d3e08a.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.FASDFASDCAS_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.FASDFASDCAS_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.FASDFASDCAS_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: 
          registryUsername: ${{ secrets.FASDFASDCAS_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.FASDFASDCAS_REGISTRY_PASSWORD }}
          containerAppName: fasdfasdcas
          resourceGroup: tmauldin-test
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          


